AddCSLuaFile();

fprp.declareChatCommand{
	command = "drop",
	description = "Drop the weapon you're holding.",
	delay = 1.5
}

fprp.declareChatCommand{
	command = "dropweapon",
	description = "Drop the weapon you're holding.",
	delay = 1.5
}

fprp.declareChatCommand{
	command = "weapondrop",
	description = "Drop the weapon you're holding.",
	delay = 1.5
}
