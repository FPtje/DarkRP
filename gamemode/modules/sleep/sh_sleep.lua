fprp.declareChatCommand({
	command = "sleep",
	description = "Go to sleep or wake up",
	delay = 1.5
});

fprp.declareChatCommand({
	command = "wake",
	description = "Go to sleep or wake up",
	delay = 1.5
});

fprp.declareChatCommand({
	command = "wakeup",
	description = "Go to sleep or wake up",
	delay = 1.5
});
